pipeline {
    agent any

    stages {
        stage('Setup') {
            steps {
                script {
                    // Ensure Python and pip are installed correctly
                    sh 'python3 -m ensurepip --default-pip'
                    sh 'python3 -m pip install --upgrade pip'
                    sh 'python3 -m pip install -r requirements.txt'
                    sh 'python3 -m pip install pyinstaller'
                }
            }
        }

        stage('Build Executable') {
            steps {
                script {
                    // Package calc.py as an executable using PyInstaller
                    sh 'python3 -m PyInstaller --onefile -w calc.py'
                }
            }
        }

        stage('Archive Artifact') {
            steps {
                archiveArtifacts artifacts: 'dist/calc*', fingerprint: true
            }
        }
    }
}
